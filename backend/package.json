{
  "name": "eventflow-backend",
  "version": "0.1.0",
  "description": "Serverless event processing pipeline - Backend",
  "main": "src/handlers/eventIngestion.js",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "lint": "eslint src/",
    "lint:fix": "eslint src/ --fix",
    "local": "sam local start-api",
    "build": "sam build",
    "deploy": "sam deploy",
    "deploy:guided": "sam deploy --guided"
  },
  "keywords": [
    "aws",
    "lambda",
    "serverless",
    "eventbridge",
    "sqs",
    "dynamodb"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^8.56.0",
    "jest": "^29.7.0"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.490.0",
    "@aws-sdk/client-sqs": "^3.490.0",
    "@aws-sdk/client-sns": "^3.490.0",
    "@aws-sdk/lib-dynamodb": "^3.490.0",
    "aws-xray-sdk-core": "^3.5.3",
    "uuid": "^9.0.0"
  }
}
